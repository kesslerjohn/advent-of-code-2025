import { pickN } from './Batteries.js';
import { readFile } from 'fs';

(() => {

//    readFile('../../inputs/day3-test.txt', 'utf8', (err, data) => {
    readFile('../../inputs/day3.txt', 'utf8', (err, data) => {
        if (err) {
            throw err;
        }

    });
    
})();
